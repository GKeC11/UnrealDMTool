{
  "name": "nooutsiders-game-server",
  "private": true,
  "version": "0.1.0",
  "description": "WebSocket GameServer for NoOutsiders.",
  "main": "dist/Server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/Server.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  }
}
