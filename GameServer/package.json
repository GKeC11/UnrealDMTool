{
  "name": "nooutsiders-game-server",
  "private": true,
  "version": "0.1.0",
  "description": "Lightweight account, lobby, and room data server for NoOutsiders.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  }
}
